checkout:
  post:
    - NOW=$(date +"%b %d %Y")
    - date
    - date +"%b %d %Y"
    - echo \<span id=\"buildDate\"\>$NOW\</span\> > updated.html
    - echo \<span id=\"buildDate\"\>$NOW\</span\>
    - echo \<span id=\"buildDate\"\>$(date +\"%b %d %Y\")\</span\>
    - git add updated.html
test:
  override:
    - git config user.name "groundh0g"
    - git config user.email "joehall@codetopia.com"
    - echo $(git commit -m "Updating build date from CI.")
    - echo $(git push)
    - php -v
